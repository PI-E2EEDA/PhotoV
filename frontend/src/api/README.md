# API client

This `Api.ts` file is generated either
- by the command `pnpm apigen` in `frontend` folder from the deployed OpenAPI spec on `https://api.photov.srd.rs/openapi.json`
- by the command `pnpm localapigen` in `frontend` folder from the local OpenAPI spec on `https://localhost:8000/openapi.json`
